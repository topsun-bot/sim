# 实施清单

## 已完成

- [x] `doc/数据回放系统方案.md` 方案文档
- [x] `replay/` Python 包（MCAP 读取、同步、MuJoCo 后端、指标）
- [x] `assets/mujoco/d1_replay.xml` H2-grasp 场景
- [x] Docker 双后端：`sim-replay:mujoco` + `sim-replay:isaac`（NGC 5.1.0）
- [x] 环境变量校验 / 配置快照 / 镜像导出（`docker/setup.sh`）
- [x] 冒烟测试（MuJoCo inspect + Isaac omni 检测）
- [x] 单 episode 完整回放（`report.json` + `tracking.csv` + 视频）
- [x] 回放实时可视化（`--visualize`：3D 视窗 + 相机窗口）
- [x] Isaac 后端 MuJoCo 委托（可跑通完整回放链路）

## 命令速查

```bash
# 本地
pip install -e .
replay inspect data/eda9cc2192f7.mcap
replay run
replay run -v                    # 实时可视化

# Docker 一键部署
bash docker/setup.sh --backend both

# Docker 回放
docker compose --env-file docker/.env run --rm replay
docker compose --env-file docker/.env --profile isaac run --rm replay-isaac
docker compose --env-file docker/.env --profile viz run --rm replay-viz

# 冒烟测试
bash docker/smoke_isaac.sh

# 单元测试
PYTHONPATH=. pytest tests/ -q
```

## Phase 2 待办

- [ ] 批处理 `replay run --data-dir`
- [ ] `follower` / `leader` 对照模式 CLI 验证
- [ ] 域随机化（摩擦、质量）
- [ ] 调参提升 `sim_success` 率（当前简化运动学模型误差较大）

## Phase 3 待办

- [ ] Isaac Sim 原生 `SimBackend`（替代 MuJoCo 委托）
- [ ] Isaac profile 视频导出（解决 GL 冲突）
- [ ] L2 回归集 batch 门禁
